# Basics-  
*Conocimientos básicos sobre C++*

## 🧾 Descripción  
Este repositorio contiene ejercicios y ejemplos organizados por temas clave de programación en C++. Su objetivo es servir como base de estudio para estudiantes de Ingeniería Informática que desean reforzar los conceptos fundamentales del lenguaje C++ y su paradigma orientado a objetos.

## 📚 Estructura del contenido  
Los directorios incluyen, entre otros:

- `estruct_condicionales/` – Ejercicios de if, switch, ternario.  
- `estruct_repetitivas/` – Bucles for, while, do-while.  
- `funciones/` – Funciones, sobrecarga, paso por valor/referencia.  
- `arrays_vectores/p1/` – Uso de arrays y vectores.  
- `matrices/` – Trabajo con matrices (bidimensionales).  
- `punteros/` – Punteros, referencias y aritmética de punteros.  
- `clases-objetos/` – Definición de clases, atributos, métodos.  
- `clasesDerivadas-Herencia-polimorfismo/` – Herencia, polimorfismo y clases derivadas.  
- `templates/` – Plantillas genéricas (templates) de funciones y clases.  
- `ordenamiento/` – Algoritmos de ordenamiento (por ejemplo, quicksort, mergesort).  
- `busquedas/` – Algoritmos de búsqueda (lineal, binaria).  
- `pilas/`, `colas/` – Estructuras de datos básicas implementadas con C++.

## ⚙️ Lenguaje y entorno  
- Lenguaje: C++ (ISO estándar)  
- Requiere un compilador C++ moderno (por ejemplo, g++ 9 o superior)  
- Compilación típica:  
  ```bash
  g++ –std=c++17 archivo.cpp –o programa
  ./programa

